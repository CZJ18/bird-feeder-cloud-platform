{
  "name": "bird-feeder-cloud-platform-deploy",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix bird-feeder-platform ci && npm --prefix bird-feeder-platform run build"
  }
}
